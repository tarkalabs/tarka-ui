import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Badge from '@/components/Badge';
import StoryTemplate from "../_template/StoryTemplate";

<Meta title="Components/Atoms/Badge" />

<StoryTemplate
    componentName="Badge"
    path="@tarkaui/atoms"
    typeDescs={[
        {
            name: `size`,
            type: `'small' | 'regular' | 'large'`,
            default: `'regular'`,
            desc: `The size of the badge.`,
        },
    ]}
    muiRef="https://mui.com/base/api/badge-unstyled/"
/>

export const Template = (args) => <Badge {...args} />;

<Canvas>
    <Story
        name="WithContent"
        args={{
            size:'large',
            badgeContent: 1
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
